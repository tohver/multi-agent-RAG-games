# Copy this file to `.env` and paste your keys between the quotes.
# The file must sit in this directory. Never commit the real .env.

OPENAI_API_KEY=""
TAVILY_API_KEY=""

# Only needed if your OpenAI access goes through a proxy that gives you a
# different base URL. Leave it out entirely when using api.openai.com.
# OPENAI_BASE_URL=""
